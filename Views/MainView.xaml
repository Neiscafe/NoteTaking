<Window x:Class="NoteTaking.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NoteTaking.Views"
        mc:Ignorable="d"
        xmlns:vm="clr-namespace:NoteTaking.Viewmodels"
        d:DataContext="{d:DesignInstance vm:MainViewModel}"
        Title="MainWindow"
        Height="450"
        Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <local:BoardUserControl Grid.Column="1" 
                                Background="BurlyWood"
                                />
        <local:BoardUserControl Margin="20,20,20,20"
                                Panel.ZIndex="1" />
        <local:BoardUserControl Grid.Column="2"
                                Margin="20,20,20,20"
                                Panel.ZIndex="2" />
    </Grid>
</Window>
